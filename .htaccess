RewriteEngine On

# Let real files and folders load normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all other requests to the root front controller
RewriteRule ^ index.php [L]